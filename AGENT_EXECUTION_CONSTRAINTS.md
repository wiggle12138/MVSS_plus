# AGENT 执行限制规定（必须严格遵守）

适用范围：本仓库内所有后续大模型/自动化代理。  
目标：避免未经许可的长时间运行、避免污染实验数据、保证代码修改可控可复现。

---

## 1) 运行代码前的强制审批规则（最高优先级）

在任何会触发程序运行、脚本执行、批量实验前，必须先向用户提交“运行计划”，并等待用户明确同意（如“同意执行/开始跑”）。

运行计划至少包含：

1. 运行命令（完整命令行）
2. 运行次数（总 run 数）
3. 参数矩阵（分片数、节点数、策略、窗口等）
4. 预期时长（粗估）
5. 预期产物（哪些日志/指标文件）
6. 输出目录（写到哪里）
7. 是否会清理运行态目录（`log/*.csv`、`record/triedb`、`*_blockchain_db`）

**未获明确同意，禁止执行任何运行命令。**

---

## 2) 长任务保护规则

1. 默认仅允许“小样本冒烟”：
   - `RUNS <= 1`
   - 参数组合不超过 2 组
2. 若超过上述规模，必须二次确认，并提示总组合数与预计总时长。
3. 若用户未明确要求，不得自行扩展为多轮（例如 10 次、100 次）批跑。
4. 若已误触发长任务，必须立即停止并向用户说明原因。

---

## 3) 实验分工与开关约束（按说明文档执行）

根据 `说明文档/期刊实验规划.md` 与相关文档：

1. 主实验（四策略对比）默认 **关闭** `EnableSyncProbe`。
2. Stage3 微基准（MVSS vs MVSS-Delta）才 **开启** `EnableSyncProbe`。
3. 实验六（窗口敏感性）优先扫描 `DeltaAggregateWindowMs`，且策略限定 `MVSS-Delta`。
4. 未实现实验（如资源 profiling / 攻击注入）不得伪造结论，必须明确标注“待开发”。

---

## 4) 代码修改约束（结合本工作区现有要求）

1. 不得回滚用户已有改动；不得擅自删除历史结果目录。
2. 修改脚本前先说明改动目标与影响范围。
3. 涉及默认参数变更（如 `params/config.go`）必须告知用户，并解释是否影响既有实验可比性。
4. 新增实验脚本应放在：
   - 入口 `.bat`：仓库根目录
   - 逻辑脚本：`scripts/`
   - 文档：`说明文档/`
5. 输出目录必须避免覆盖旧数据，使用独立实验根目录。
6. 未经用户要求，不执行 git 提交/推送。

---

## 5) 数据落盘与目录规范

推荐结构（必须保持“按实验/参数/轮次”可追溯）：

```text
results/
  expX_name/
    raw/
      shards{S}_nodes{N}/
        param_{V}/
          run{K}/
            *.csv
            *.out.log
            *.err.log
            run_meta.txt
    metrics/
      *.json
    summary/
      *.csv
      *.md
      figures/*.png
```

每个 `run` 必须有 `run_meta.txt`，记录关键参数：

- strategy
- dataset
- shard_num
- nodes_per_shard
- 关键实验参数（如 `delta_window_ms`）
- run_index
- 注入规模/探针参数

---

## 6) 执行后汇报模板（必须）

每次运行后必须反馈：

1. 实际执行了哪些命令
2. 实际跑了多少组、多少次
3. 成功/失败组数
4. 产物路径
5. 下一步建议（继续扩大规模 / 修复脚本 / 汇总分析）

---

## 7) 违例处理

若代理违反本规范（尤其“未获同意直接跑实验”）：

1. 立即停止任务；
2. 明确说明已执行内容与影响范围；
3. 等待用户决定是否保留或清理已产生数据；
4. 之后严格按本文件执行。

